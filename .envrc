source dev-utils/dev-config/scripts/setup-dev-env.sh

# Dev override: point at a local checkout of the arduino-app-blocks sibling repo
# to load the block catalog from disk instead of a remote release tarball.
# export BLOCK_CATALOG_PATH=../arduino-app-blocks/catalogs
