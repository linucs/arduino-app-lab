id: builtin_spi
category: "Messaging::SPI"
docs:
  library: "https://www.arduino.cc/reference/en/language/functions/communication/spi/"
implementations:
  - runtime: "arduino:cpp"
    codegen:
      imports:
        - "#include <SPI.h>"
    blocks:
      # --- SPI.begin (place inside Setup container) ---
      - blockly:
          type: cpp_spi_begin
          message0: "SPI.begin"
          args0: []
          previousStatement: null
          nextStatement: null
          tooltip: "Initialize the SPI bus. Place inside the Setup container."
          helpUrl: "https://www.arduino.cc/reference/en/language/functions/communication/spi/begin/"
        codegen:
          body:
            - "SPI.begin();"

      # --- SPI.beginTransaction ---
      - blockly:
          type: cpp_spi_settings
          message0: "SPI.beginTransaction speed %1 %2 %3"
          args0:
            - type: input_value
              name: SPEED
              check: ["Number", "int", "unsigned int", "long", "unsigned long", "byte", "word", "uint8_t", "uint16_t", "uint32_t", "int8_t", "int16_t", "int32_t"]
            - type: field_dropdown
              name: BIT_ORDER
              options:
                - ["MSBFIRST", "MSBFIRST"]
                - ["LSBFIRST", "LSBFIRST"]
            - type: field_dropdown
              name: MODE
              options:
                - ["SPI_MODE0", "SPI_MODE0"]
                - ["SPI_MODE1", "SPI_MODE1"]
                - ["SPI_MODE2", "SPI_MODE2"]
                - ["SPI_MODE3", "SPI_MODE3"]
          previousStatement: null
          nextStatement: null
          tooltip: "Configure and begin an SPI transaction."
          helpUrl: "https://www.arduino.cc/reference/en/language/functions/communication/spi/begintransaction/"
          inputsInline: true
        codegen:
          body:
            - "SPI.beginTransaction(SPISettings({{SPEED}}, {{BIT_ORDER}}, {{MODE}}));"
          inputDefaults:
            SPEED: "1000000"

      # --- SPI.transfer ---
      - blockly:
          type: cpp_spi_transfer
          message0: "SPI.transfer %1"
          args0:
            - type: input_value
              name: VALUE
              check: ["Number", "int", "unsigned int", "long", "unsigned long", "byte", "word", "uint8_t", "uint16_t", "uint32_t", "int8_t", "int16_t", "int32_t"]
          output: Number
          tooltip: "Send and receive one byte over SPI."
          helpUrl: "https://www.arduino.cc/reference/en/language/functions/communication/spi/transfer/"
          inputsInline: true
        codegen:
          body:
            - "SPI.transfer({{VALUE}})"
          precedence: ATOMIC
          inputDefaults:
            VALUE: "0"

      # --- SPI.endTransaction ---
      - blockly:
          type: cpp_spi_end_transaction
          message0: "SPI.endTransaction"
          args0: []
          previousStatement: null
          nextStatement: null
          tooltip: "End an SPI transaction."
          helpUrl: "https://www.arduino.cc/reference/en/language/functions/communication/spi/endtransaction/"
        codegen:
          body:
            - "SPI.endTransaction();"

      # --- SPI.end ---
      - blockly:
          type: cpp_spi_end
          message0: "SPI.end"
          args0: []
          previousStatement: null
          nextStatement: null
          tooltip: "Disable the SPI bus."
          helpUrl: "https://www.arduino.cc/reference/en/language/functions/communication/spi/end/"
        codegen:
          body:
            - "SPI.end();"
